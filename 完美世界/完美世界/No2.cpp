#include <iostream>
#include <string>

using namespace std;

int main() {
	//n为题数,score接收每个题的份数，time接收每个题花费的时间，total_time为考试总时间
	string n;
	string score;
	string time;
	string total_time;
	int max_socre = 0;
	while (getline(cin, n)) {
		getline(cin, score);
		getline(cin, time);
		getline(cin, total_time);

		//输入完成

	}
}